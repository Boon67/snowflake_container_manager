# Snowflake Container Manager - Docker Environment Configuration
# Copy this file to .env and fill in your Snowflake credentials

# Snowflake Connection Settings
SNOWFLAKE_ACCOUNT=your-account.region
SNOWFLAKE_USER=your-username
SNOWFLAKE_WAREHOUSE=your-warehouse
SNOWFLAKE_DATABASE=your-database
SNOWFLAKE_SCHEMA=your-schema

# Authentication - Choose ONE method:
# Method 1: Password Authentication
SNOWFLAKE_PASSWORD=your-password

# Method 2: Keypair Authentication (comment out SNOWFLAKE_PASSWORD above)
# SNOWFLAKE_PRIVATE_KEY_PATH=/app/backend/secrets/snowflake_private_key.pem

# JWT Settings (optional - will be auto-generated if not provided)
JWT_SECRET_KEY=your-jwt-secret-key
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application Settings
# These are optional and have sensible defaults