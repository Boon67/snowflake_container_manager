# Snowflake Database Configuration
# Replace with your actual Snowflake account details
SNOWFLAKE_ACCOUNT=your_account.region
SNOWFLAKE_USER=your_username
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=APP
SNOWFLAKE_SCHEMA=CONFIG
SNOWFLAKE_ROLE=ACCOUNTADMIN

# Snowflake Authentication Method 1: Password Authentication
# Use this for simple username/password authentication
SNOWFLAKE_PASSWORD=your_password

# Snowflake Authentication Method 2: Keypair Authentication (More Secure)
# Use this for private/public key authentication
# Comment out SNOWFLAKE_PASSWORD and uncomment these lines to use keypair auth:
# SNOWFLAKE_PRIVATE_KEY_PATH=/path/to/your/private_key.p8
# SNOWFLAKE_PRIVATE_KEY_PASSPHRASE=your_key_passphrase_if_encrypted

# Application Security
# IMPORTANT: Change this to a strong, unique secret key in production
SECRET_KEY=your-secret-key-change-this-in-production

# Authentication
# Default login credentials for the web interface
DEFAULT_USERNAME=admin
DEFAULT_PASSWORD=password123

# Application Configuration
# Environment: development, staging, production
ENVIRONMENT=development

# Logging Configuration
LOG_LEVEL=INFO

# Server Configuration (optional)
# HOST=0.0.0.0
# PORT=8000

# CORS Configuration (optional)
# Comma-separated list of allowed origins for CORS
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Database Connection Pool (optional)
# Maximum number of database connections
# MAX_DB_CONNECTIONS=10

# JWT Token Configuration (optional)
# Token expiration time in minutes
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# Frontend Configuration (for production builds)
# FRONTEND_BUILD_PATH=frontend/build

# External Services (add as needed)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password

# Monitoring and Analytics (optional)
# SENTRY_DSN=your_sentry_dsn_here
# ANALYTICS_API_KEY=your_analytics_key_here
